import random

nameString = input("Give me everybody's names, separated by a comma.\n")
nameList = names_string.split(", ")

victim = (random.choice(names)).capitalize()
print(victim +' is going to pay!') 

# This specific output is for deciding who pays a bill.
